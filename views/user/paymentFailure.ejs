<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed - ChronoX</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .failure-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        
        .failure-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            padding: 40px;
            text-align: center;
            max-width: 500px;
            width: 100%;
            position: relative;
            overflow: hidden;
        }
        
        .failure-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: linear-gradient(90deg, #ff6b6b, #ee5a24);
        }
        
        .failure-icon {
            width: 100px;
            height: 100px;
            background: linear-gradient(135deg, #ff6b6b, #ee5a24);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 30px;
            animation: pulse 2s infinite;
        }
        
        .failure-icon i {
            font-size: 50px;
            color: white;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }
        
        .failure-title {
            color: #2c3e50;
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 15px;
        }
        
        .failure-message {
            color: #7f8c8d;
            font-size: 16px;
            margin-bottom: 30px;
            line-height: 1.6;
        }
        
        .order-info {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
            margin: 20px 0;
            border-left: 4px solid #ff6b6b;
        }
        
        .order-info h6 {
            color: #2c3e50;
            margin-bottom: 10px;
            font-weight: 600;
        }
        
        .order-info p {
            margin: 5px 0;
            color: #7f8c8d;
        }
        
        .action-buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 30px;
        }
        
        .btn-retry {
            background: linear-gradient(135deg, #667eea, #764ba2);
            border: none;
            color: white;
            padding: 12px 30px;
            border-radius: 25px;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }
        
        .btn-retry:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
            color: white;
        }
        
        .btn-orders {
            background: white;
            border: 2px solid #667eea;
            color: #667eea;
            padding: 12px 30px;
            border-radius: 25px;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }
        
        .btn-orders:hover {
            background: #667eea;
            color: white;
            transform: translateY(-2px);
        }
        
        .help-section {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #ecf0f1;
        }
        
        .help-section p {
            color: #95a5a6;
            font-size: 14px;
            margin-bottom: 10px;
        }
        
        .help-contact {
            color: #667eea;
            text-decoration: none;
            font-weight: 600;
        }
        
        .help-contact:hover {
            color: #764ba2;
        }
        
        @media (max-width: 576px) {
            .failure-card {
                padding: 30px 20px;
                margin: 10px;
            }
            
            .action-buttons {
                flex-direction: column;
                align-items: center;
            }
            
            .btn-retry, .btn-orders {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="failure-container">
        <div class="failure-card">
            <div class="failure-icon">
                <i class="fas fa-times"></i>
            </div>
            
            <h1 class="failure-title">Payment Failed</h1>
            <p class="failure-message">
                We're sorry, but your payment could not be processed. Don't worry, no amount has been charged from your account.
            </p>
            
            <% if (order) { %>
            <div class="order-info">
                <h6><i class="fas fa-receipt me-2"></i>Order Details</h6>
                <p><strong>Order ID:</strong> #<%= order._id.toString().slice(-8).toUpperCase() %></p>
                <p><strong>Amount:</strong> ₹<%= order.totalAmount %></p>
                <p><strong>Status:</strong> <span class="text-danger">Payment Failed</span></p>
            </div>
            <% } %>
            
            <div class="action-buttons">
                <% if (order) { %>
                <a href="/retry-payment/<%= order._id %>" class="btn-retry">
                    <i class="fas fa-redo"></i>
                    Retry Payment
                </a>
                <% } else { %>
                <a href="/cart" class="btn-retry">
                    <i class="fas fa-shopping-cart"></i>
                    Go to Cart
                </a>
                <% } %>
                
                <a href="/my-orders" class="btn-orders">
                    <i class="fas fa-list"></i>
                    View Orders
                </a>
            </div>
            
            <div class="help-section">
                <p>Need help with your payment?</p>
                <a href="/contact" class="help-contact">
                    <i class="fas fa-headset me-1"></i>
                    Contact Support
                </a>
            </div>
        </div>
    </div>

    <!-- SweetAlert2 -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Show any toast messages if they exist
        <% if (typeof toast !== 'undefined' && toast) { %>
            <% if (toast.type === 'error') { %>
                Swal.fire({
                    icon: 'error',
                    title: 'Error',
                    text: '<%= toast.message %>',
                    toast: true,
                    position: 'top-end',
                    showConfirmButton: false,
                    timer: 5000,
                    timerProgressBar: true
                });
            <% } %>
        <% } %>
    </script>
</body>
</html>
