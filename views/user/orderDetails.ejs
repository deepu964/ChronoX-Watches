<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Details - ChronoX</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
        }

        .header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 20px;
        }

        .order-title {
            font-size: 28px;
            font-weight: 700;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .order-status {
            padding: 8px 20px;
            border-radius: 20px;
            font-weight: 600;
            font-size: 14px;
            text-transform: uppercase;
        }

        .status-placed {
            background: linear-gradient(135deg, #4CAF50, #45a049);
            color: white;
        }

        .status-processing {
            background: linear-gradient(135deg, #ff9800, #f57c00);
            color: white;
        }

        .status-shipped {
            background: linear-gradient(135deg, #2196F3, #1976D2);
            color: white;
        }

        .status-delivered {
            background: linear-gradient(135deg, #4CAF50, #388E3C);
            color: white;
        }

        .back-btn {
            padding: 10px 20px;
            background: transparent;
            border: 2px solid #667eea;
            color: #667eea;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .back-btn:hover {
            background: #667eea;
            color: white;
            transform: translateY(-2px);
        }

        .details-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-bottom: 30px;
        }

        .detail-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .card-title {
            font-size: 18px;
            font-weight: 700;
            color: #333;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .card-title i {
            color: #667eea;
        }

        .detail-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
            padding-bottom: 15px;
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        }

        .detail-row:last-child {
            margin-bottom: 0;
            padding-bottom: 0;
            border-bottom: none;
        }

        .detail-label {
            color: #666;
            font-weight: 500;
        }

        .detail-value {
            color: #333;
            font-weight: 600;
        }

        .items-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .order-item {
            display: flex;
            align-items: center;
            gap: 20px;
            padding: 20px;
            background: rgba(102, 126, 234, 0.05);
            border-radius: 12px;
            margin-bottom: 15px;
            transition: all 0.3s ease;
        }

        .order-item:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
        }

        .item-image {
            width: 80px;
            height: 80px;
            border-radius: 10px;
            overflow: hidden;
            flex-shrink: 0;
        }

        .item-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .item-details {
            flex: 1;
        }

        .item-name {
            font-size: 16px;
            font-weight: 600;
            color: #333;
            margin-bottom: 8px;
        }

        .item-info {
            display: flex;
            gap: 20px;
            color: #666;
            font-size: 14px;
        }

        .item-price {
            font-size: 18px;
            font-weight: 700;
            color: #667eea;
        }

        .total-section {
            background: rgba(102, 126, 234, 0.1);
            border-radius: 12px;
            padding: 20px;
            margin-top: 20px;
        }

        .total-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
        }

        .total-row:last-child {
            margin-bottom: 0;
            padding-top: 15px;
            border-top: 2px solid #667eea;
            font-size: 18px;
            font-weight: 700;
            color: #667eea;
        }

        .action-buttons {
            display: flex;
            gap: 15px;
            justify-content: center;
            margin-top: 30px;
            flex-wrap: wrap;
        }

        .btn {
            padding: 12px 25px;
            border: none;
            border-radius: 10px;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s ease;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
        }

        .btn-secondary {
            background: transparent;
            color: #667eea;
            border: 2px solid #667eea;
        }

        .btn-secondary:hover {
            background: #667eea;
            color: white;
        }

        @media (max-width: 768px) {
            .details-grid {
                grid-template-columns: 1fr;
                gap: 20px;
            }

            .header-content {
                flex-direction: column;
                align-items: flex-start;
            }

            .order-item {
                flex-direction: column;
                text-align: center;
                gap: 15px;
            }

            .item-info {
                justify-content: center;
            }

            .detail-row {
                flex-direction: column;
                align-items: flex-start;
                gap: 5px;
            }

            .action-buttons {
                flex-direction: column;
                align-items: center;
            }

            .btn {
                width: 100%;
                max-width: 300px;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <%- include("../partials/user/header") %>
    
    <div class="container">
        <!-- Header -->
        <div class="header">
            <div class="header-content">
                <div>
                    <h1 class="order-title">Order Details</h1>
                    <p style="color: #666; margin-top: 5px;">Order #<%= order._id.toString().slice(-8).toUpperCase() %></p>
                </div>
                <div style="display: flex; align-items: center; gap: 15px;">
                    <span class="order-status status-<%= order.status.toLowerCase() %>">
                        <%= order.status %>
                    </span>
                    <a href="/my-orders" class="back-btn">
                        <i class="fas fa-arrow-left"></i>
                        Back to Orders
                    </a>
                </div>
            </div>
        </div>

        <!-- Order Details Grid -->
        <div class="details-grid">
            <!-- Order Information -->
            <div class="detail-card">
                <h3 class="card-title">
                    <i class="fas fa-info-circle"></i>
                    Order Information
                </h3>
                <div class="detail-row">
                    <span class="detail-label">Order Date:</span>
                    <span class="detail-value"><%= new Date(order.createdAt).toLocaleDateString('en-IN', { 
                        year: 'numeric', 
                        month: 'long', 
                        day: 'numeric',
                        hour: '2-digit',
                        minute: '2-digit'
                    }) %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Payment Method:</span>
                    <span class="detail-value"><%= order.paymentMethod === 'COD' ? 'Cash on Delivery' : 'Online Payment' %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Order Status:</span>
                    <span class="detail-value"><%= order.status %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Total Items:</span>
                    <span class="detail-value"><%= order.items.length %> item(s)</span>
                </div>
            </div>

            <!-- Delivery Address -->
            <div class="detail-card">
                <h3 class="card-title">
                    <i class="fas fa-map-marker-alt"></i>
                    Delivery Address
                </h3>
                <div class="detail-row">
                    <span class="detail-label">Name:</span>
                    <span class="detail-value"><%= order.address.fullName %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Phone:</span>
                    <span class="detail-value"><%= order.address.phone %></span>
                </div>
                <div class="detail-row">
                    <span class="detail-label">Address:</span>
                    <span class="detail-value">
                        <%= order.address.addressLine %><br>
                        <%= order.address.city %>, <%= order.address.state %><br>
                        PIN: <%= order.address.pincode %>
                    </span>
                </div>
            </div>
        </div>

        <!-- Order Items -->
        <div class="items-card">
            <h3 class="card-title">
                <i class="fas fa-shopping-bag"></i>
                Order Items (<%= order.items.length %>)
            </h3>
            
            <% order.items.forEach(item => { %>
                <div class="order-item">
                    <div class="item-image">
                        <% if (item.product.images && item.product.images.length > 0) { %>
                            <img src="https://res.cloudinary.com/<%= cloudName %>/image/upload/w_80,h_80,c_fill/<%= item.product.images[0].public_id %>" 
                                 alt="<%= item.product.name %>" 
                                 onerror="this.src='/images/placeholder.jpg'">
                        <% } else { %>
                            <img src="/images/placeholder.jpg" alt="No image">
                        <% } %>
                    </div>
                    <div class="item-details">
                        <div class="item-name"><%= item.product.name %></div>
                        <div class="item-info">
                            <span>Quantity: <%= item.quantity %></span>
                            <span>Price: ₹<%= item.price.toLocaleString() %></span>
                        </div>
                    </div>
                    <div class="item-price">₹<%= (item.price * item.quantity).toLocaleString() %></div>
                </div>
            <% }) %>

            <!-- Order Total -->
            <div class="total-section">
                <div class="total-row">
                    <span>Subtotal:</span>
                    <span>₹<%= order.totalAmount.toLocaleString() %></span>
                </div>
                <div class="total-row">
                    <span>Shipping:</span>
                    <span>₹50</span>
                </div>
                <div class="total-row">
                    <span>Total Amount:</span>
                    <span>₹<%= (order.totalAmount + 50).toLocaleString() %></span>
                </div>
            </div>
        </div>

        <!-- Return Status Section -->
        <% if (returnRequest) { %>
            <div class="return-status-section" style="margin: 30px 0; padding: 25px; border-radius: 12px;
                 <% if (returnRequest.status === 'Approved' || returnRequest.status === 'Cash Refund Processed') { %>
                     background: linear-gradient(135deg, #d4edda, #c3e6cb); border: 2px solid #28a745;
                 <% } else if (returnRequest.status === 'Rejected') { %>
                     background: linear-gradient(135deg, #f8d7da, #f5c6cb); border: 2px solid #dc3545;
                 <% } else { %>
                     background: linear-gradient(135deg, #fff3cd, #ffeaa7); border: 2px solid #ffc107;
                 <% } %>">

                <div style="display: flex; align-items: center; margin-bottom: 20px;">
                    <% if (returnRequest.status === 'Approved' || returnRequest.status === 'Cash Refund Processed') { %>
                        <i class="fas fa-check-circle" style="font-size: 24px; color: #28a745; margin-right: 15px;"></i>
                    <% } else if (returnRequest.status === 'Rejected') { %>
                        <i class="fas fa-times-circle" style="font-size: 24px; color: #dc3545; margin-right: 15px;"></i>
                    <% } else { %>
                        <i class="fas fa-clock" style="font-size: 24px; color: #ffc107; margin-right: 15px;"></i>
                    <% } %>
                    <h3 style="margin: 0; color: #333;">Return Request Status</h3>
                </div>

                <div class="return-info-grid" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; margin-bottom: 20px;">
                    <div>
                        <strong>Status:</strong>
                        <span style="display: inline-block; margin-left: 10px; padding: 5px 12px; border-radius: 20px; font-size: 12px; font-weight: 600;
                              <% if (returnRequest.status === 'Approved' || returnRequest.status === 'Cash Refund Processed') { %>
                                  background: #28a745; color: white;
                              <% } else if (returnRequest.status === 'Rejected') { %>
                                  background: #dc3545; color: white;
                              <% } else { %>
                                  background: #ffc107; color: #333;
                              <% } %>">
                            <%= returnRequest.status %>
                        </span>
                    </div>

                    <div>
                        <strong>Refund Amount:</strong>
                        <span style="font-size: 18px; font-weight: 600; color: #28a745; margin-left: 10px;">
                            ₹<%= returnRequest.totalRefundAmount.toLocaleString('en-IN') %>
                        </span>
                    </div>

                    <div>
                        <strong>Requested On:</strong>
                        <span style="margin-left: 10px;">
                            <%= new Date(returnRequest.requestedAt).toLocaleDateString('en-IN', {
                                year: 'numeric',
                                month: 'long',
                                day: 'numeric'
                            }) %>
                        </span>
                    </div>

                    <% if (returnRequest.reviewedAt) { %>
                    <div>
                        <strong>Reviewed On:</strong>
                        <span style="margin-left: 10px;">
                            <%= new Date(returnRequest.reviewedAt).toLocaleDateString('en-IN', {
                                year: 'numeric',
                                month: 'long',
                                day: 'numeric'
                            }) %>
                        </span>
                    </div>
                    <% } %>
                </div>

                <% if (returnRequest.status === 'Approved') { %>
                    <div class="refund-message" style="background: white; padding: 15px; border-radius: 8px; border-left: 4px solid #28a745;">
                        <h4 style="color: #28a745; margin: 0 0 10px 0;">
                            <i class="fas fa-money-bill-wave"></i> Your Return Has Been Approved!
                        </h4>
                        <p style="margin: 0; color: #333;">
                            Great news! Your return request has been approved. The refund amount of
                            <strong>₹<%= returnRequest.totalRefundAmount.toLocaleString('en-IN') %></strong>
                            is being processed to your wallet.
                        </p>
                    </div>
                <% } else if (returnRequest.status === 'Refunded') { %>
                    <div class="refund-message" style="background: white; padding: 15px; border-radius: 8px; border-left: 4px solid #28a745;">
                        <h4 style="color: #28a745; margin: 0 0 10px 0;">
                            <i class="fas fa-check-double"></i> Refund Completed!
                        </h4>
                        <p style="margin: 0; color: #333;">
                            Your refund of <strong>₹<%= returnRequest.totalRefundAmount.toLocaleString('en-IN') %></strong>
                            has been credited to your wallet successfully.
                            <% if (returnRequest.refundedAt) { %>
                                <br><small style="color: #666;">
                                    Completed on: <%= new Date(returnRequest.refundedAt).toLocaleDateString('en-IN', {
                                        year: 'numeric',
                                        month: 'long',
                                        day: 'numeric'
                                    }) %>
                                </small>
                            <% } %>
                            <br><a href="/wallet" style="color: #28a745; text-decoration: none; font-weight: 600;">
                                <i class="fas fa-wallet"></i> View My Wallet
                            </a>
                        </p>
                    </div>
                <% } else if (returnRequest.status === 'Rejected') { %>
                    <div class="refund-message" style="background: white; padding: 15px; border-radius: 8px; border-left: 4px solid #dc3545;">
                        <h4 style="color: #dc3545; margin: 0 0 10px 0;">
                            <i class="fas fa-exclamation-triangle"></i> Return Request Rejected
                        </h4>
                        <p style="margin: 0; color: #333;">
                            Unfortunately, your return request has been rejected.
                            <% if (returnRequest.adminNotes) { %>
                                <br><strong>Reason:</strong> <%= returnRequest.adminNotes %>
                            <% } %>
                        </p>
                    </div>
                <% } else { %>
                    <div class="refund-message" style="background: white; padding: 15px; border-radius: 8px; border-left: 4px solid #ffc107;">
                        <h4 style="color: #856404; margin: 0 0 10px 0;">
                            <i class="fas fa-hourglass-half"></i> Return Under Review
                        </h4>
                        <p style="margin: 0; color: #333;">
                            Your return request is being reviewed by our team. You will receive an update within 24-48 hours.
                            If approved, you will receive a cash refund of
                            <strong>₹<%= returnRequest.totalRefundAmount.toLocaleString('en-IN') %></strong>.
                        </p>
                    </div>
                <% } %>

                <% if (returnRequest.reason) { %>
                    <div style="margin-top: 15px; padding: 10px; background: rgba(255,255,255,0.7); border-radius: 5px;">
                        <strong>Return Reason:</strong> <%= returnRequest.reason %>
                        <% if (returnRequest.description) { %>
                            <br><strong>Additional Details:</strong> <%= returnRequest.description %>
                        <% } %>
                    </div>
                <% } %>
            </div>
        <% } %>

        <!-- Action Buttons -->
        <div class="action-buttons">
            <a href="/shop" class="btn btn-primary">
                <i class="fas fa-shopping-bag"></i>
                Continue Shopping
            </a>
            <a href="/my-orders" class="btn btn-secondary">
                <i class="fas fa-list"></i>
                View All Orders
            </a>
            <% if (order.status === 'Delivered' && !returnRequest) { %>
                <button class="btn btn-warning" onclick="openReturnModal()">
                    <i class="fas fa-undo"></i>
                    Request Return
                </button>
            <% } %>
        </div>
    </div>

    <!-- Return Modal -->
    <% if (order.status === 'Delivered') { %>
    <div id="returnModal" class="modal" style="display: none;">
        <div class="modal-content">
            <div class="modal-header">
                <h3>Request Return</h3>
                <span class="close" onclick="closeReturnModal()">&times;</span>
            </div>
            <div class="modal-body">
                <form id="returnForm">
                    <div class="form-group">
                        <label for="returnReason">Reason for Return:</label>
                        <select id="returnReason" name="reason" required>
                            <option value="">Select a reason</option>
                            <option value="Defective Product">Defective Product</option>
                            <option value="Wrong Item Received">Wrong Item Received</option>
                            <option value="Size/Color Issue">Size/Color Issue</option>
                            <option value="Product Not as Described">Product Not as Described</option>
                            <option value="Damaged During Shipping">Damaged During Shipping</option>
                            <option value="Changed Mind">Changed Mind</option>
                            <option value="Other">Other</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="returnDescription">Additional Details (Optional):</label>
                        <textarea id="returnDescription" name="description" rows="3"
                                  placeholder="Please provide additional details about the return..."></textarea>
                    </div>

                    <div class="form-group">
                        <label>Items to Return:</label>
                        <div class="return-items">
                            <% order.items.forEach((item, index) => { %>
                                <div class="return-item">
                                    <input type="checkbox" id="item_<%= index %>" name="items"
                                           value="<%= item.product._id %>" data-price="<%= item.price %>"
                                           data-max-qty="<%= item.quantity %>" checked>
                                    <label for="item_<%= index %>">
                                        <span class="item-name"><%= item.product.name %></span>
                                        <span class="item-price">₹<%= item.price.toLocaleString() %> each</span>
                                    </label>
                                    <div class="quantity-selector">
                                        <label>Qty:</label>
                                        <input type="number" name="quantity_<%= index %>"
                                               min="1" max="<%= item.quantity %>" value="<%= item.quantity %>"
                                               class="qty-input">
                                    </div>
                                </div>
                            <% }) %>
                        </div>
                    </div>

                    <div class="refund-summary">
                        <strong>Estimated Refund: ₹<span id="refundAmount">0</span></strong>
                    </div>

                    <div class="modal-actions">
                        <button type="button" class="btn btn-secondary" onclick="closeReturnModal()">Cancel</button>
                        <button type="submit" class="btn btn-warning">Submit Return Request</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <% } %>

    <%- include("../partials/user/footer") %>

    <script>
        function openReturnModal() {
            document.getElementById('returnModal').style.display = 'block';
            calculateRefund();
        }

        function closeReturnModal() {
            document.getElementById('returnModal').style.display = 'none';
        }

        function calculateRefund() {
            let total = 0;
            const checkboxes = document.querySelectorAll('input[name="items"]:checked');

            checkboxes.forEach((checkbox, index) => {
                const price = parseFloat(checkbox.dataset.price);
                const qtyInput = document.querySelector(`input[name="quantity_${index}"]`);
                const quantity = parseInt(qtyInput.value) || 0;
                total += price * quantity;
            });

            document.getElementById('refundAmount').textContent = total.toLocaleString();
        }

        // Add event listeners for quantity changes and checkbox changes
        document.addEventListener('DOMContentLoaded', function() {
            const checkboxes = document.querySelectorAll('input[name="items"]');
            const qtyInputs = document.querySelectorAll('.qty-input');

            checkboxes.forEach(checkbox => {
                checkbox.addEventListener('change', calculateRefund);
            });

            qtyInputs.forEach(input => {
                input.addEventListener('input', calculateRefund);
            });
        });

        // Handle form submission
        document.getElementById('returnForm').addEventListener('submit', function(e) {
            e.preventDefault();

            const formData = new FormData(this);
            const selectedItems = [];
            const checkboxes = document.querySelectorAll('input[name="items"]:checked');

            checkboxes.forEach((checkbox, index) => {
                const qtyInput = document.querySelector(`input[name="quantity_${index}"]`);
                selectedItems.push({
                    productId: checkbox.value,
                    quantity: parseInt(qtyInput.value)
                });
            });

            if (selectedItems.length === 0) {
                alert('Please select at least one item to return');
                return;
            }

            const returnData = {
                orderId: '<%= order._id %>',
                reason: formData.get('reason'),
                description: formData.get('description'),
                items: selectedItems
            };

            fetch('/request-return', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(returnData)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    alert(data.message);
                    closeReturnModal();
                    location.reload();
                } else {
                    alert(data.message || 'Failed to submit return request');
                }
            })
            .catch(error => {
                console.error('Error:', error);
                alert('An error occurred while submitting the return request');
            });
        });

        // Close modal when clicking outside
        window.onclick = function(event) {
            const modal = document.getElementById('returnModal');
            if (event.target === modal) {
                closeReturnModal();
            }
        }
    </script>

    <style>
        .modal {
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0,0,0,0.5);
        }

        .modal-content {
            background-color: white;
            margin: 5% auto;
            padding: 0;
            border-radius: 10px;
            width: 90%;
            max-width: 600px;
            max-height: 80vh;
            overflow-y: auto;
        }

        .modal-header {
            padding: 20px;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .modal-header h3 {
            margin: 0;
            color: #333;
        }

        .close {
            font-size: 28px;
            font-weight: bold;
            cursor: pointer;
            color: #aaa;
        }

        .close:hover {
            color: #000;
        }

        .modal-body {
            padding: 20px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: 600;
            color: #333;
        }

        .form-group select,
        .form-group textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 14px;
        }

        .return-items {
            border: 1px solid #eee;
            border-radius: 5px;
            padding: 15px;
        }

        .return-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid #f0f0f0;
        }

        .return-item:last-child {
            border-bottom: none;
        }

        .return-item label {
            flex: 1;
            margin-left: 10px;
            margin-bottom: 0;
        }

        .item-name {
            display: block;
            font-weight: 600;
        }

        .item-price {
            display: block;
            color: #666;
            font-size: 12px;
        }

        .quantity-selector {
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .qty-input {
            width: 60px;
            padding: 5px;
            border: 1px solid #ddd;
            border-radius: 3px;
            text-align: center;
        }

        .refund-summary {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            text-align: center;
            margin: 20px 0;
            font-size: 16px;
        }

        .modal-actions {
            display: flex;
            gap: 10px;
            justify-content: flex-end;
            margin-top: 20px;
        }

        .btn.btn-warning {
            background-color: #f39c12;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
        }

        .btn.btn-warning:hover {
            background-color: #e67e22;
        }
    </style>
</body>
</html>
