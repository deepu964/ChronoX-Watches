<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopping Cart</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            
        }

        .cart-container {
            max-width: 800px;
            margin-top: 30px;
            margin-left: auto;
            margin-right: auto;
            margin-bottom: 30px;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            transition: transform 0.3s ease;
        }

        .cart-container:hover {
            transform: translateY(-5px);
        }

        .cart-header {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            padding: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .cart-header::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
            transform: rotate(45deg);
            animation: shimmer 3s infinite;
        }

        @keyframes shimmer {

            0%,
            100% {
                transform: rotate(45deg) translateX(-100%);
            }

            50% {
                transform: rotate(45deg) translateX(100%);
            }
        }

        .cart-title {
            font-size: 2rem;
            font-weight: 700;
            letter-spacing: 1px;
            z-index: 1;
        }

        .empty-cart-btn {
            background: rgba(255, 255, 255, 0.2);
            border: 2px solid rgba(255, 255, 255, 0.3);
            color: white;
            padding: 12px 20px;
            border-radius: 25px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s ease;
            backdrop-filter: blur(10px);
            z-index: 1;
        }

        .empty-cart-btn:hover {
            background: rgba(255, 255, 255, 0.3);
            border-color: rgba(255, 255, 255, 0.5);
            transform: scale(1.05);
        }

        .cart-items {
            padding: 0;
        }

        .cart-item {
            display: flex;
            align-items: center;
            padding: 30px;
            border-bottom: 1px solid #f0f0f0;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .cart-item::before {
            content: '';
            position: absolute;
            left: -100%;
            top: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(103, 126, 234, 0.05), transparent);
            transition: left 0.5s ease;
        }

        .cart-item:hover {
            background: #f9f9f9;
            transform: translateX(10px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .cart-item:hover::before {
            left: 100%;
        }

        .item-image {
            width: 80px;
            height: 80px;
            border-radius: 15px;
            overflow: hidden;
            margin-right: 20px;
            position: relative;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
            transition: all 0.3s ease;
        }

        .item-image:hover {
            transform: scale(1.1) rotate(5deg);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
        }

        .item-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .item-image:hover img {
            transform: scale(1.2);
        }

        .item-details {
            flex: 1;
            margin-right: 20px;
        }

        .item-name {
            font-size: 1.2rem;
            font-weight: 600;
            color: #333;
            margin-bottom: 8px;
            transition: color 0.3s ease;
        }

        .cart-item:hover .item-name {
            color: #000000;
        }

        .item-price {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .current-price {
            font-size: 1.1rem;
            font-weight: 700;
            color: #000000;
        }

        .original-price {
            font-size: 0.9rem;
            color: #999;
            text-decoration: line-through;
        }

        .quantity-controls {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-right: 20px;
        }

        .quantity-btn {
            width: 40px;
            height: 40px;
            border: 2px solid #000000;
            background: white;
            color: #000000;
            border-radius: 50%;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            font-weight: bold;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .quantity-btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: #000000;
            border-radius: 50%;
            transition: all 0.3s ease;
            transform: translate(-50%, -50%);
        }

        .quantity-btn:hover::before {
            width: 100%;
            height: 100%;
        }

        .quantity-btn:hover {
            color: white;
            transform: scale(1.1);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.4);
        }

        .quantity-display {
            font-size: 1.2rem;
            font-weight: 600;
            min-width: 30px;
            text-align: center;
            padding: 8px 16px;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            border-radius: 20px;
            border: 2px solid #dee2e6;
        }

        .remove-btn {
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
            color: white;
            border: none;
            padding: 12px 20px;
            border-radius: 25px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .remove-btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            transition: all 0.3s ease;
            transform: translate(-50%, -50%);
        }

        .remove-btn:hover::before {
            width: 300%;
            height: 300%;
        }

        .remove-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 25px rgba(255, 107, 107, 0.4);
        }

        .cart-summary {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            padding: 30px;
            border-top: 3px solid #000000;
        }

        .promo-section {
            margin-bottom: 25px;
            text-align: center;
        }

        .promo-link {
            color: #000000;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.9rem;
            transition: all 0.3s ease;
            position: relative;
        }

        .promo-link::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 50%;
            width: 0;
            height: 2px;
            background: #000000;
            transition: all 0.3s ease;
            transform: translateX(-50%);
        }

        .promo-link:hover::after {
            width: 100%;
        }

        .promo-link:hover {
            transform: translateY(-2px);
        }

        .subtotal {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 1.3rem;
            font-weight: 700;
            color: #333;
            margin-bottom: 20px;
            padding: 20px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        }

       

        .cart-actions {
            display: flex;
            gap: 39px;
            align-items: center;
            justify-content: center;
            padding: 19px;
        }

        .cart-actions button {
            flex: 1;
            padding: 18px;
            border: none;
            border-radius: 15px;
            font-size: 1.1rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .checkout-btn {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
        }

        .checkout-btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            transition: all 0.3s ease;
            transform: translate(-50%, -50%);
        }

        .checkout-btn:hover::before {
            width: 300%;
            height: 300%;
        }

        .checkout-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.4);
        }

        .continue-btn {
            background: rgb(218, 213, 213);
            color: #000000;
            border: 2px solid #000000;
        }

        .continue-btn:hover {
            background: #000000;
            color: white;
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        /* Cart item warning styles */
        .cart-item.warning {
            border-left: 4px solid #ffc107;
            background-color: #fff3cd;
        }

        .cart-item.error {
            border-left: 4px solid #dc3545;
            background-color: #f8d7da;
        }

        .item-status-badge {
            display: inline-block;
            padding: 2px 8px;
            border-radius: 12px;
            font-size: 0.75rem;
            font-weight: bold;
            margin-left: 10px;
        }

        .badge-warning {
            background-color: #ffc107;
            color: #212529;
        }

        .badge-error {
            background-color: #dc3545;
            color: white;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            /* body {
                padding: 10px;
            } */

            .cart-container {
                margin: 10px 10px;
                border-radius: 15px;
                
            }

            .cart-header {
                padding: 20px;
                flex-direction: column;
                gap: 15px;
                text-align: center;
            }

            .cart-title {
                font-size: 1.5rem;
            }

            .cart-item {
                flex-direction: column;
                padding: 20px;
                text-align: center;
                gap: 15px;
            }

            .item-image {
                margin-right: 0;
                margin-bottom: 10px;
            }

            .item-details {
                margin-right: 0;
                margin-bottom: 15px;
            }

            .quantity-controls {
                margin-right: 0;
                margin-bottom: 15px;
            }

            .cart-actions {
                flex-direction: column;
            }

            .cart-actions button {
                padding: 15px;
                font-size: 1rem;
            }
        }

        @media (max-width: 480px) {
            .cart-item {
                padding: 15px;
            }

            .item-image {
                width: 60px;
                height: 60px;
            }

            .cart-title {
                font-size: 1.3rem;
            }

            .quantity-btn {
                width: 35px;
                height: 35px;
                font-size: 1rem;
            }

            .quantity-display {
                font-size: 1rem;
                padding: 6px 12px;
            }
        }

        /* Animation for item removal */
        .cart-item.removing {
            animation: slideOut 0.5s ease-out forwards;
        }

        @keyframes slideOut {
            0% {
                transform: translateX(0);
                opacity: 1;
                max-height: 140px;
            }

            50% {
                transform: translateX(-100%);
                opacity: 0;
            }

            100% {
                transform: translateX(-100%);
                opacity: 0;
                max-height: 0;
                padding: 0;
                margin: 0;
            }
        }

        /* Loading animation */
        .loading {
            position: relative;
            pointer-events: none;
        }

        .loading::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 20px;
            height: 20px;
            border: 2px solid #f3f3f3;
            border-top: 2px solid #000000;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            transform: translate(-50%, -50%);
        }

        @keyframes spin {
            0% {
                transform: translate(-50%, -50%) rotate(0deg);
            }

            100% {
                transform: translate(-50%, -50%) rotate(360deg);
            }
        }

        .price-summary {

            border-radius: 8px;
            padding: 16px;
            margin-top: 20px;
            background: #fff;
            width: 100%;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            ;
        }

        .price-summary h3 {
            font-size: 18px;
            margin-bottom: 16px;
            color: #212121;
        }

        .price-row {
            display: flex;
            justify-content: space-between;
            margin: 8px 0;
        }

        .price-row span,
        .price-row strong {
            font-size: 14px;
            color: #333;
        }

        .price-row .green {
            color: #388e3c;
            font-weight: 500;
        }

        .price-row .red {
            color: #d32f2f;
            font-weight: 500;
            cursor: pointer;
        }

        .price-row.total {
            font-size: 18px;
            font-weight: bold;
            margin-top: 12px;
            border-top: 1px solid #ccc;
            padding-top: 10px;
        }

        hr {
            margin: 12px 0;
            border: none;
            border-top: 1px solid #e0e0e0;
        }


        .subtotal h3 {
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 16px;
            color: #212121;
        }

        .price-row {
            display: flex;
            justify-content: space-between;
            margin: 8px 0;
        }

        .price-row strong {
            font-size: 20px;
            padding: 5px;
        }

        .price-row span {
            font-size: 17px;
        }

        .price-row.total {
            margin-top: 16px;
            font-size: 18px;
            font-weight: bold;
            border-top: 1px solid #ddd;
            padding-top: 12px;
        }

        .price-row span:nth-child(2) {
            font-weight: 500;
        }

        hr {
            margin: 19px 0;
            border: none;
            border-top: 1px solid #eee;
        }

        .price-row .green {
            color: #388e3c;
        }

        .price-row .red {
            color: #d32f2f;
            cursor: pointer;
        }

        
        .empty-cart {
            text-align: center;
            padding: 80px 40px;
            background: white;
            border-radius: 20px;
            margin: 20px;
        }

        .empty-cart-icon {
            font-size: 50px;
            color: #e0e0e0;
            margin-bottom: 30px;
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }

        .empty-cart h2 {
            font-size: 2.5rem;
            color: #333;
            margin-bottom: 15px;
            font-weight: 700;
        }

        .empty-cart p {
            font-size: 1.2rem;
            color: #666;
            margin-bottom: 40px;
            line-height: 1.6;
        }

        .empty-cart-actions {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .empty-cart-btn {
            padding: 15px 30px;
            border: none;
            border-radius: 25px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            position: relative;
            overflow: hidden;
        }

        .shop-now-btn {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }

        .shop-now-btn:hover {
            transform: translateY(-3px);
            background:#667eea;
            color: white;
            box-shadow: 0 15px 35px rgba(8, 46, 217, 0.4);
        }

        .wishlist-btn {
            background: white;
            color: #667eea;
            border: 2px solid #667eea;
        }

        .wishlist-btn:hover {
            background: #667eea;
            color: white;
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.3);
        }

        .empty-cart-features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(1px, 1fr));
            gap: 30px;
            margin-top: 60px;
            padding: 40px 0;
            border-top: 2px solid #f0f0f0;
        }

        .feature-item {
            text-align: center;
            padding: 20px;
        }

        .feature-icon {
            font-size: 30px;
            color: #667eea;
            margin-bottom: 15px;
        }

        .feature-title {
            font-size: 1.1rem;
            font-weight: 600;
            color: #333;
            margin-bottom: 8px;
        }

        .feature-desc {
            font-size: 0.9rem;
            color: #666;
            line-height: 1.4;
        }

        /* Hide cart summary when empty */
        .cart-container.empty .price-summary,
        .cart-container.empty .cart-actions {
            display: none;
        }

        /* Responsive for empty cart */
        @media (max-width: 768px) {
            .empty-cart {
                padding: 60px 20px;
                margin: 10px;
            }

            .empty-cart-icon {
                font-size: 80px;
            }

            .empty-cart h2 {
                font-size: 2rem;
            }

            .empty-cart p {
                font-size: 1rem;
            }

            .empty-cart-actions {
                flex-direction: column;
                align-items: center;
            }

            .empty-cart-btn {
                width: 100%;
                max-width: 300px;
                justify-content: center;
            }

            .empty-cart-features {
                grid-template-columns: 1fr;
                gap: 20px;
                margin-top: 40px;
            }
        }
    </style>
</head>

<body>
    <%-include("../partials/user/header") %>
        <div class="cart-container <%= items.length === 0 ? 'empty' : '' %>">
            <% if (items.length === 0) { %>
                <!-- Empty Cart Display -->
                <div class="empty-cart">
                    <div class="empty-cart-icon">
                        <i class="fas fa-shopping-cart"></i>
                    </div>
                    <h2>Your Cart is Empty</h2>
                    <p>Looks like you haven't added anything to your cart yet.<br>
                       Start shopping to fill it up with amazing products!</p>

                    <div class="empty-cart-actions">
                        <a href="/shop" class="empty-cart-btn shop-now-btn">
                            <i class="fas fa-shopping-bag"></i>
                            Start Shopping
                        </a>
                        <a href="/wish-list" class="empty-cart-btn wishlist-btn">
                            <i class="fas fa-heart"></i>
                            View Wishlist
                        </a>
                    </div>

                    <div class="empty-cart-features">
                        <div class="feature-item">
                            <div class="feature-icon">
                                <i class="fas fa-truck"></i>
                            </div>
                            
                            <div class="feature-desc">Free delivery on orders above ₹500</div>
                        </div>
                        <div class="feature-item">
                            <div class="feature-icon">
                                <i class="fas fa-shield-alt"></i>
                            </div>
                            <div class="feature-title">Secure Payment</div>
                            <div class="feature-desc">100% secure payment methods</div>
                        </div>
                        <div class="feature-item">
                            <div class="feature-icon">
                                <i class="fas fa-undo"></i>
                            </div>
                            <div class="feature-title">Easy Returns</div>
                            <div class="feature-desc">Hassle-free returns within 7 days</div>
                        </div>
                        <div class="feature-item">
                            <div class="feature-icon">
                                <i class="fas fa-headset"></i>
                            </div>
                            <div class="feature-title">24/7 Support</div>
                            <div class="feature-desc">Customer support available anytime</div>
                        </div>
                    </div>
                </div>
            <% } else { %>
                <!-- Regular Cart Display -->
                <div class="cart-header">
                    <h1 class="cart-title">CART</h1>
                    <button class="empty-cart-btn" onclick="emptyCart()">Empty My Cart</button>
                </div>

                <div class="cart-items" id="cartItems">
                    <% items.forEach((item, index)=> { %>
                        <div class="cart-item" data-id="<%= item.product._id %>">
                            <div class="product-image">
                                <% const image=item.product.images?.[0]?.public_id; %>
                                    <% if (image) { %>
                                        <img src="https://res.cloudinary.com/<%= cloudName %>/image/upload/w_100,h_100,c_fill/<%= image %>"
                                            alt="<%= item.product.name %>" width="100" height="100"
                                            onerror="this.onerror=null; this.src='/images/placeholder.jpg';" />
                                        <% } else { %>
                                            <img src="/images/placeholder.jpg" alt="No image available" width="50"
                                                height="50">
                                            <% } %>
                            </div>

                            <div class="item-details" style="padding: 2%;">
                                <div class="item-name">
                                    <%= item.product.name %>
                                </div>
                                <div class="item-price">
                                    <% const price=item.product.variants[0].salePrice ||
                                        item.product.variants[0].regularPrice; const
                                        originalPrice=item.product.variants[0].regularPrice; %>
                                        <span class="current-price">₹<%= price.toLocaleString() %></span>
                                        <% if (price !==originalPrice) { %>
                                            <span class="original-price">₹<%= originalPrice.toLocaleString() %></span>
                                            <% } %>
                                </div>
                            </div>
                            <div class="quantity-controls">
                                <button class="quantity-btn"
                                    onclick="updateCart('<%= item.product._id %>', 'decrease')">−</button>
                                <div class="quantity-display" id="qty-<%= item.product._id %>">
                                    <%= item.quantity %>
                                </div>
                                <button class="quantity-btn"
                                    onclick="updateCart('<%= item.product._id %>', 'increase')">+</button>
                            </div>
                            <button class="remove-btn" onclick="removeItem('<%= item.product._id %>')">Remove</button>
                        </div>
                        <% }) %>
                </div>
            <% } %>

                <!-- Price Summary and Actions (only show when cart has items) -->
                <% if (items.length > 0) { %>
                    <!-- <div class="cart-summary">
                    <div class="promo-section">
                        <a href="#" class="promo-link">Save 10%</a>
                    </div> -->
                    <div class="price-summary">
                        <h3>PRICE DETAILS (<%= items.length %> Items)</h3>

                        <div class="price-row">
                            <span>Total MRP</span>
                            <span id="cart-total">₹<%= totalMRP.toLocaleString() %></span>
                        </div>

                        <div class="price-row">
                            <span>Discount on MRP</span>
                            <span class="green" id="discount">- ₹<%= discount.toLocaleString() %></span>
                        </div>

                        <!-- <div class="price-row">
                            <span>Coupon Discount</span>
                            <span class="red">Apply Coupon</span>
                        </div> -->
                        <hr />

                        <div class="price-row total">
                            <strong>Total Amount</strong>
                            <strong id="grandTotal">₹<%= grandTotal  %></strong>
                        </div>
                    </div>

                    <div class="cart-actions">
                        <button class="checkout-btn" onclick="checkOut()">Proceed to checkout</button>
                        <noscript>
                            <a href="/check-out" style="display: inline-block; margin-top: 10px;">
                                <button class="checkout-btn">Proceed to checkout (No JS)</button>
                            </a>
                        </noscript>
                        <a href="/shop"><button class="continue-btn">Continue Shopping</button></a>
                    </div>
                <% } %>
        </div>
        </div>
        <%-include("../partials/user/footer") %>

            <script>
                

                async function updateCart(productId, action) {
                    try {
                            const res = await fetch(`/cart/update`, {
                                method: 'POST',
                                headers: { 'Content-Type': 'application/json' },
                                body: JSON.stringify({ productId, action })
                            });
                            const data = await res.json();

                            if (data.success) {
                                // Update quantity
                                document.getElementById(`qty-${productId}`).innerText = data.quantity;

                                if (data.cartTotal !== undefined) {
                                    document.getElementById('cart-total').innerText = `₹${data.cartTotal.toLocaleString()}`;
                                }

                                if(data.discountOff !== undefined){
                                    document.getElementById('discount').innerText = `-₹${data.discountOff.toLocaleString()}`
                                }

                                if(data.grandTotal !== undefined){
                                    document.getElementById('grandTotal').innerText = `${data.grandTotal.toLocaleString()}`
                                }

                                showToast(data.message || 'Cart updated');
                            } else {
                                showToast(data.message || 'Failed to update cart', 'error');
                            }
                        } catch (err) {
                            console.error(err);
                            showToast('Something went wrong');
                        }
                    }


                async function removeItem(productId) {
                    try {
                        const res = await fetch(`/cart/remove/${productId}`, {
                            method: 'POST'
                        });
                        const data = await res.json();
                        if (data.success) {
                            const cartItem = document.querySelector(`[data-id="${productId}"]`);
                            if (cartItem) {
                                cartItem.classList.add('removing');
                                setTimeout(() => {
                                    cartItem.remove();
                                    // Check if cart is now empty
                                    const remainingItems = document.querySelectorAll('.cart-item').length;
                                    if (remainingItems <= 1) { // <= 1 because we just removed one
                                        location.reload(); // Reload to show empty cart
                                    }
                                }, 500);
                            }
                            showToast('Product removed from cart');
                            setTimeout(() => location.reload(), 1000);
                        } else {
                            showToast(data.message || 'Failed to remove item');
                        }
                    } catch (err) {
                        console.error(err);
                        showToast('Something went wrong');
                    }
                }


                async function checkOut(){
                    try {
                        // Show loading state
                        const checkoutBtn = document.querySelector('.checkout-btn');
                        const originalText = checkoutBtn.innerHTML;
                        checkoutBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Validating...';
                        checkoutBtn.disabled = true;

                        // First validate the cart
                        const response = await fetch('/validate-cart', {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json'
                            }
                        });

                        const result = await response.json();

                        // Reset button state
                        checkoutBtn.innerHTML = originalText;
                        checkoutBtn.disabled = false;

                        if (!result.success) {
                            // Show SweetAlert error based on the type of error
                            if (result.type === 'invalid_products') {
                                Swal.fire({
                                    icon: 'error',
                                    title: 'Products Unavailable',
                                    html: `
                                        <p>The following items are no longer available:</p>
                                        <ul style="text-align: left; margin: 10px 0;">
                                            ${result.invalidItems.map(item => `<li><strong>${item}</strong></li>`).join('')}
                                        </ul>
                                        <p>Please remove them from your cart to continue.</p>
                                    `,
                                    confirmButtonColor: '#3085d6',
                                    confirmButtonText: 'Update Cart'
                                }).then(() => {
                                    window.location.reload();
                                });
                            } else if (result.type === 'out_of_stock') {
                                Swal.fire({
                                    icon: 'warning',
                                    title: 'Items Out of Stock',
                                    html: `
                                        <p>The following items don't have enough stock:</p>
                                        <ul style="text-align: left; margin: 10px 0;">
                                            ${result.outOfStockItems.map(item =>
                                                `<li><strong>${item.name}</strong><br>
                                                <small>Available: ${item.available}, Requested: ${item.requested}</small></li>`
                                            ).join('')}
                                        </ul>
                                        <p>Please update quantities or remove them from your cart.</p>
                                    `,
                                    confirmButtonColor: '#3085d6',
                                    confirmButtonText: 'Update Cart'
                                }).then(() => {
                                    window.location.reload();
                                });
                            } else {
                                // Generic error
                                Swal.fire({
                                    icon: 'error',
                                    title: 'Checkout Error',
                                    text: result.message || 'Unable to proceed to checkout',
                                    confirmButtonColor: '#3085d6'
                                });
                            }
                        } else {
                            // Validation successful, proceed to checkout
                            window.location.href = "/check-out";
                        }

                    } catch (error) {
                        console.error('Checkout validation error:', error);

                        // Reset button state
                        const checkoutBtn = document.querySelector('.checkout-btn');
                        checkoutBtn.innerHTML = 'Proceed to checkout';
                        checkoutBtn.disabled = false;

                        Swal.fire({
                            icon: 'error',
                            title: 'Connection Error',
                            text: 'Unable to validate your cart. Please check your connection and try again.',
                            confirmButtonColor: '#3085d6'
                        });
                    }
                }

                // Function to check cart validity on page load
                async function checkCartValidity() {
                    try {
                        const response = await fetch('/validate-cart', {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json'
                            }
                        });

                        const result = await response.json();

                        if (!result.success) {
                            // Show a warning toast about cart issues
                            if (result.type === 'invalid_products' || result.type === 'out_of_stock') {
                                showWarningToast('Some items in your cart may not be available. Please review before checkout.');
                            }
                        }
                    } catch (error) {
                        console.error('Cart validity check error:', error);
                    }
                }

                // Check cart validity when page loads
                document.addEventListener('DOMContentLoaded', function() {
                    checkCartValidity();
                });

                function emptyCart() {
                    showConfirmDialog(
                        'Empty Cart?',
                        'Are you sure you want to empty your cart? This action cannot be undone.',
                        'Yes, Empty Cart',
                        'Cancel'
                    ).then((result) => {
                        if (result.isConfirmed) {
                            // Add loading state
                            const emptyBtn = document.querySelector('.empty-cart-btn');
                            const originalText = emptyBtn.innerHTML;
                            emptyBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Emptying...';
                            emptyBtn.disabled = true;

                        fetch('/cart/empty', {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json'
                            }
                        }).then(response => {
                            if (response.ok) {
                                showToast('Cart emptied successfully');
                                // Add fade out animation to all cart items
                                const cartItems = document.querySelectorAll('.cart-item');
                                cartItems.forEach((item, index) => {
                                    setTimeout(() => {
                                        item.classList.add('removing');
                                    }, index * 100);
                                });
                                setTimeout(() => location.reload(), 1000);
                            } else {
                                showToast('Failed to empty cart');
                                emptyBtn.innerHTML = originalText;
                                emptyBtn.disabled = false;
                            }
                        }).catch(error => {
                            console.error('Error:', error);
                            showToast('Something went wrong');
                            emptyBtn.innerHTML = originalText;
                            emptyBtn.disabled = false;
                        });
                        }
                    });
                }

                const style = document.createElement('style');
                style.textContent = `
            .toast {
                visibility: hidden;
                min-width: 250px;
                margin-left: -125px;
                background-color: #333;
                color: #fff;
                text-align: center;
                border-radius: 2px;
                padding: 16px;
                position: fixed;
                z-index: 1;
                left: 50%;
                bottom: 30px;
                font-size: 17px;
                opacity: 0;
                transition: opacity 0.5s, bottom 0.5s;
            }

            .toast.show {
                visibility: visible;
                opacity: 1;
                bottom: 50px;
            }
        `;
                document.head.appendChild(style);
            </script>
</body>

</html>